[tools]
go = "1.27.1"

[env]
_.path = ["bin"]

[tasks.build]
description = "Build every command into bin/"
run = "go build -o bin/ ./cmd/..."

[tasks.test]
description = "Run the tests"
run = "go test ./..."
raw = true

[tasks.lint]
description = "Check the formatting and vet the packages"
run = ["test -z \"$(gofmt -l .)\"", "go vet ./..."]

[tasks.fmt]
description = "Format the code"
run = "gofmt -w ."

[tasks.check]
description = "Lint and test"
depends = ["lint", "test"]

[tasks.delivery]
description = "Prove phase 1 of the delivery layer: relay, USB stick, refusal, parts, tail"
run = "bash scripts/test-delivery.sh"
raw = true

[tasks.check-relay]
description = "Prove that one running relay carries sealed data and live calls"
run = "bash scripts/check-relay.sh"
raw = true

[tasks.check-clean-build]
description = "Prove that each release build of a commit stamps vcs.modified=false"
run = "bash scripts/check-clean-build.sh"
raw = true

[tasks.interface]
description = "Prove phases A to D of the capability interface"
run = "bash scripts/test-interface.sh"
raw = true
