[tools]
go = "1.27.1"

[env]
_.path = ["bin"]

[tasks.build]
description = "Build every command into bin/"
run = "go build -o bin/ ./cmd/..."

[tasks.test]
description = "Run the tests"
run = "go test ./..."
raw = true

[tasks.lint]
description = "Check the formatting and vet the packages"
run = ["test -z \"$(gofmt -l .)\"", "go vet ./..."]

[tasks.fmt]
description = "Format the code"
run = "gofmt -w ."

[tasks.check]
description = "Lint and test"
depends = ["lint", "test"]

[tasks.cli]
description = "Run the whole of ARC in Go: relay, citizen, provider, caller"
run = "bash scripts/test-go-cli.sh"
raw = true

[tasks.delivery]
description = "Prove phase 1 of the delivery layer: relay, USB stick, refusal, parts, tail"
run = "bash scripts/test-delivery.sh"
raw = true

[tasks.interface]
description = "Prove phases A to C of the capability interface: calls, sealed data, messages and groups"
run = "bash scripts/test-interface.sh"
raw = true
