# Higgsfield torch preview

Completed preview: `torch-preview.mp4`.

Higgsfield generated `torch-raw.mp4` using Kling 3.0, standard mode, silent, with identical source artwork as the start and end references. Exact settings and job ID are in `generation.json`.

The finished preview places only the generated flame region over the original engraving, using a feathered mask. This preserves the original body, cloak, landscape, and parchment. The flame itself remains softer than the original ink lines. No extra generation was needed for compositing.

Verified: 5 seconds, 1536 x 1024, 24 frames per second, no audio, 933805 bytes. Frame sampling showed flame motion. Pixel comparisons found negligible frame-to-frame change in the preserved body region. The start and end were inspected numerically; a perfectly invisible repeat is not established by these checks.

The available credit balance fell from 110 to 102.5. The separately reported trial-credit counter stayed at 100, so the service's exact internal balance allocation was not established. No additional purchase was made.

The approved preview is copied unchanged to `../../prometheus-torch.mp4` and used on the site's cover and Prometheus chapter. The original still remains available when motion is paused, reduced motion is requested, scripts are unavailable, or video playback fails. Video loads only when the artwork becomes visible and pauses when it leaves view or the browser tab is hidden. A shared pause/play control is available while animated artwork is visible. FAL was not used.

Site verification: desktop and phone layouts inspected in a local Chrome browser; playback, pause/resume, off-screen pause, initial and live reduced-motion settings, and failed-video fallback passed. The site has not been deployed by this task.
